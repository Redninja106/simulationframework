namespace SimulationFramework.Tests;

[TestClass]
public class TestCollision
{
    // [TestMethod]
    // [DataRow(1, 1, 3, 3, 2, 2)]
    // public void CollideLinePoint(float fromX, float fromY, float toX, float toY, float pointX, float pointY)
    // {
    //     Assert.IsTrue(Collision.CollideLinePoint(new Vector2(fromX, fromY), new Vector2(toX, toY), new(pointX, pointY)));
    // }
}